# Copies des tickets

- [`initial/`](initial/) — texte Lab 1 (commit `f201a50`)
- [`final/`](final/) — texte Autonomy 1 (après correction des tickets)
- `002.diff` … `005.diff` — `diff -u` initiale → finale

Le 001 est le même dans les deux dossiers.
